version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    # volumes:
    #   - ./backend/usr/app/node_modules
    #   - ./backend:/usr/app
  app:
    build: ./app
    ports:
      - "3000:3000"
    tty: true
    # volumes:
    #   - ./app/usr/app/node_modules
    #   - ./app:/usr/app
    # depends_on:
    #   - backend
  admin:
    build: ./admin
    ports:
      - "3001:3001"
    tty: true
    # volumes:
    #   - ./admin/usr/app/node_modules
    #   - ./admin:/usr/app
    # depends_on:
    #   - app
  # landing:
  #   build: ./landing
  #   ports:
  #     - "3002:3002"
  #   stdin_open: true
  #   volumes:
  #     - ./landing/usr/app/node_modules
  #     - ./landing:/usr/app
  #   depends_on:
  #     - admin
